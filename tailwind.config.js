/** @type {import('tailwindcss').Config} */
module.exports = {
  // Scan only the application source tree to keep builds predictable.
  content: ['./src/**/*.{js,jsx,ts,tsx,mdx}'],
  theme: {
    extend: {},
  },
  plugins: [],
};
